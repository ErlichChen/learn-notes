{
    "tasks": [
        {
            "type": "shell",
            "label": "ccompile",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "test.out"
            ],
            "options": {
							"cwd": "${fileDirname}"
						},
        }
    ],
    "version": "2.0.0"
}