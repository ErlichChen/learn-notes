# Merge VS Rebase

## Merge

```shell
git checkout feature
git merge master
# or
git merge master feature
```

## Rebase

```shell
git checkout feature
git rebase master
```

```shell
git checkout feature
git rebase -i master
```